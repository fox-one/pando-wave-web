declare namespace API {
  export interface Asset {
    asset_id: string;
    symbol: string;
    name: string;
    icon_url: string;
  }

  export interface Product {
    a?: any;
  }

  export interface Fiats {
    a?: any;
  }

  export interface Profile {
    a?: any;
  }
}
